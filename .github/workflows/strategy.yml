name: Deploy to server con Matrix

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        message: ["Hola", "CodigoFacilito"]

    env:
      NAME: CodigoFacilito

    steps:
      - name: Print Message
        run: echo "${{ matrix.message }}"